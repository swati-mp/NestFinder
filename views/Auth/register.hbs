<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nest Finder - Register</title>
    <link rel="stylesheet" href="/home.css">
    <link rel="stylesheet" href="/register.css">
</head>

<body>
    <div class="container">
        <header class="header">
            <nav class="nav">
                <div class="logo">
                    <img class="logo-circle" src="/assets/img/Shicon.webp" alt="Logo">
                    <h1>Nest Finder</h1>
                </div>
                <div class="nav-links">
                    <a href="/">Home</a>
                    <a href="/aboutus">About Us</a>
                    <a href="/auth/login">Login</a>
                    <a href="/auth/register">SignUp</a>
                </div>
            </nav>
        </header>

        <!-- Register Section -->
        <main class="register-section">
            <h2 class="register-title">Register</h2>
            <div id="successMessage" class="success-message">
                {{!-- 🎉 Registration Successful! --}}
                {{success}}
            </div>

            {{!-- If you Dont want otp register Feature Then Use Below Code  --}}
            {{!-- <form id="registerForm" class="register-form" action="/auth/register" method="post">
                <div class="form-group">
                    <label for="name">Name</label>
                    <input type="text" id="name" name="name" placeholder="Enter your name" required>
                </div>
                <div class="form-group">
                    <label for="role">Select Role</label>
                    <select name="role" id="role" required> 
                        <option value="">Select</option>
                        <option value="owner">Owner</option>
                        <option value="seeker">Seeker</option>
                    </select>
                </div>
                <div class="form-group">
                    <label for="email">Email</label>
                    <input type="email" id="email" name="email" placeholder="Enter your email" required>
                </div>
                <div class="form-group">
                    <label for="password">Password</label>
                    <input type="password" id="password" name="password" placeholder="Enter your password" required>
                </div>
                <button type="submit" class="submit-btn">Register</button>
            </form> --}}
            {{!-- If you Dont want otp register Feature Then Use Above Code and comment below code 66 to 103  --}}


            {{!-- If you want otp register Feature Then Use Below Code  --}}
            <!-- Step 1: Send OTP Form -->
            <form id="registerForm" class="register-form" action="/auth/send-otp" method="post">
                <div class="form-group">
                    <label for="name">Name</label>
                    <input type="text" id="name" name="name" value="{{name}}" placeholder="Enter your name" required>
                </div>
                <div class="form-group">
                    <label for="role">Select Role</label>
                    <select name="role" id="role" required>
                        {{#if role}}
                        <option value="{{role}}" selected>{{role}}</option>
                        {{/if}}
                        <option value="">Select</option>
                        <option value="owner">Owner</option>
                        <option value="seeker">Seeker</option>
                    </select>
                </div>
                <div class="form-group">
                    <label for="email">Email</label>
                    <input type="email" id="email" name="email" value="{{email}}" placeholder="Enter your email" required>
                </div>
                <button type="submit" class="submit-btn">Send OTP</button>
            </form>

            <!-- Step 2: Verify OTP and Register Form -->
            <form id="verifyOtpForm" class="register-form" action="/auth/register" method="post">
                <input type="hidden" id="email" name="email" value="{{email}}">
                <input type="hidden" id="role" name="role" value="{{role}}"> 
                <input type="hidden" id="name" name="name" value="{{name}}">
                <div class="form-group">
                    <label for="otp">OTP</label>
                    <input type="text" id="otp" name="otp" placeholder="Enter the OTP" required>
                </div>
                <div class="form-group">
                    <label for="password">Password</label>
                    <input type="password" id="password" name="password" placeholder="Enter your password" required>
                </div>
                <button type="submit" class="submit-btn">Register</button>
            </form>
            {{!-- If you want otp register Feature Then Use Above Code  --}}
        </main>
    </div>
</body>

</html>